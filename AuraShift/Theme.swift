//import SwiftUI

//enum Theme {
    // Основные цвета
  //  static let background = Color(red: 0.98, green: 0.95, blue: 0.90)  // Тёплый бежевый фон
    //static let surface = Color(red: 0.95, green: 0.92, blue: 0.85)     // Светлый беж для карточек
    //static let accent = Color(red: 0.65, green: 0.45, blue: 0.35)      // Шоколадный акцент
    //static let accentLight = Color(red: 0.75, green: 0.55, blue: 0.45) // Светлый шоколад
    //static let text = Color(red: 0.25, green: 0.20, blue: 0.15)        // Тёмный шоколад для текста
    //static let secondaryText = Color(red: 0.45, green: 0.40, blue: 0.35) // Карамельный для втор. текста
    //static let border = Color(red: 0.85, green: 0.80, blue: 0.75)      // Бежевая граница
    
    // Цвета для финансов
    //static let positive = Color(red: 0.55, green: 0.70, blue: 0.55)    // Мятный для доходов
    //static let negative = Color(red: 0.80, green: 0.55, blue: 0.55)    // Приглушённый красный
    //static let tips = Color(red: 0.85, green: 0.65, blue: 0.45)        // Карамельный для чаевых
//}
